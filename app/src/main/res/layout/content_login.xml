<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/content_login"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="ajlyfe.lectureapp.Activity.LoginActivity"
    tools:showIn="@layout/activity_login">

    <TextView android:id="@+id/MyAccountTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Welcome."
        android:fontFamily="sans-serif-thin"
        android:textSize="28sp"
        android:layout_centerHorizontal="true"
        />

    <Button
        android:text="Student"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/MyAccountTitle"
        android:layout_toStartOf="@+id/MyAccountTitle"
        android:layout_marginTop="168dp"
        android:id="@+id/studentButton" />

    <Button
        android:text="Teacher"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignTop="@+id/studentButton"
        android:layout_toEndOf="@+id/MyAccountTitle"
        android:id="@+id/teacherButton" />

</RelativeLayout>
